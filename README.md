# Red Tetris

## Network

Set url in `params.js` to be accessible across network

## Dev

`$ npm run client-dev` and `$ npm run srv-dev`

or

`docker-compose up`

## Test

### Socket

Open two terminals:

`$ npm run srv-dev`

`$ npm test`
