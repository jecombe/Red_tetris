import React from 'react';

const FooterBar = props => {
    return (
        <div style={style.FooterBarStyle}>
            Footer
        </div>
    );
};

const style = {
    FooterBarStyle: {
        border: '1px solid black'
    }
}

export default FooterBar;